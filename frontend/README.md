# React + Vite

This is the basic frontend for the project.

To run download it then in the frontend open a terminal and run the following
"npm install" this install all requried packages

Then
"npm run dev"
to start the server
